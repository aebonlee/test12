# Phase 0: 전체 구조 재설계 계획서 v2

> **작성일**: 2026-01-26
> **핵심 아이디어**: 홈은 3단계까지, 평가법별 페이지는 4~14단계 진행
> **목적**: 여러 평가법 동시 신청 가능, 평가법별 독립적 진행

---

## 1. 핵심 아이디어 💡

### 문제 상황
```
고객: "DCF, 상대가치평가법, 본질가치평가법 3개 다 해주세요"

기존 설계의 문제:
❌ 하나의 프로젝트 = 하나의 평가법만 가능
❌ 여러 평가법 신청하려면 프로젝트 3개 생성?
❌ 프로세스가 어디서 진행되는지 불명확
```

### 해결책 ⭐
```
홈 화면 (valuation.html):
  1단계: 서비스 안내
  2단계: 평가 신청 (여러 평가법 동시 선택)
  3단계: 관리자 승인 대기

평가법별 페이지 (guide-dcf.html 등):
  4단계: 평가 기초자료 제출
  5단계: 데이터 수집 중
  ...
  14단계: 평가보고서 수령

상태:
  🟢 승인됨 → 4~14단계 진행 가능
  🟡 대기중 → 3단계에서 멈춤
  ⚫ 신청안함 → "신청하지 않은 평가법입니다" 표시
```

---

## 2. 프로세스 분리 구조

### 2.1 홈 화면 (valuation.html) - 공통 프로세스

**역할**: 평가법 선택 및 관리자 승인까지

```
┌─────────────────────────────────────────────────┐
│ valuation.html (홈)                             │
├─────────────────────────────────────────────────┤
│                                                 │
│ 1단계: 서비스 안내                               │
│   → 5개 평가법 전체 소개                         │
│   → "시작하기" 버튼                              │
│                                                 │
│ 2단계: 평가 신청                                 │
│   → project-create.html로 이동                  │
│   → 여러 평가법 체크박스 선택 가능               │
│   ✅ DCF                                        │
│   ✅ 상대가치평가법                              │
│   ❌ 본질가치평가법                              │
│   → "신청하기" 클릭                              │
│                                                 │
│ 3단계: 관리자 승인 대기                          │
│   → approval-waiting.html로 이동                │
│   → 신청한 평가법들의 승인 상태 표시             │
│   DCF: ✅ 승인됨                                │
│   상대가치: 🟡 대기중                            │
│                                                 │
└─────────────────────────────────────────────────┘
```

### 2.2 평가법별 페이지 - 개별 프로세스

**역할**: 승인된 평가법의 실제 작업 진행

```
┌─────────────────────────────────────────────────┐
│ guide-dcf.html (DCF 평가법 페이지)               │
├─────────────────────────────────────────────────┤
│                                                 │
│ 상태 체크:                                       │
│   → projects 테이블에서 dcf_status 확인          │
│   → approved가 아니면 "승인 대기중" 표시         │
│                                                 │
│ 사이드바 (14단계 프로세스):                      │
│   1단계: 서비스 안내 (현재 페이지)               │
│   2단계: 평가 신청                               │
│   3단계: 관리자 승인 대기                        │
│   ────────────────────────                      │
│   4단계: 평가 기초자료 제출  ← 여기서부터 진행!  │
│   5단계: 데이터 수집 중                          │
│   ...                                           │
│   14단계: 평가보고서 수령                        │
│                                                 │
└─────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────┐
│ guide-relative.html (상대가치평가법 페이지)      │
├─────────────────────────────────────────────────┤
│                                                 │
│ 상태 체크:                                       │
│   → projects 테이블에서 relative_status 확인     │
│   → pending → "관리자 승인 대기 중입니다" 표시   │
│   → 4단계 이후 버튼 비활성화                     │
│                                                 │
└─────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────┐
│ guide-intrinsic.html (본질가치평가법 페이지)     │
├─────────────────────────────────────────────────┤
│                                                 │
│ 상태 체크:                                       │
│   → projects 테이블에서 intrinsic_status 확인    │
│   → not_requested → "신청하지 않은 평가법" 표시  │
│   → "평가 신청하기" 버튼 표시                    │
│                                                 │
└─────────────────────────────────────────────────┘
```

---

## 3. 데이터베이스 구조

### 3.1 projects 테이블 수정

```sql
-- 기존 필드
id UUID PRIMARY KEY
user_id UUID REFERENCES users(id)
company_name TEXT
project_id TEXT UNIQUE
created_at TIMESTAMP

-- 추가 필드
status TEXT  -- 'pending', 'in_progress', 'completed'
current_step INTEGER  -- 1~14 (홈 기준)

-- 평가법별 상태 필드 (신규) ⭐
dcf_status TEXT  -- 'not_requested', 'pending', 'approved', 'in_progress', 'completed'
dcf_step INTEGER  -- DCF 진행 단계 (4~14)

relative_status TEXT
relative_step INTEGER

intrinsic_status TEXT
intrinsic_step INTEGER

asset_status TEXT
asset_step INTEGER

inheritance_tax_status TEXT
inheritance_tax_step INTEGER
```

### 3.2 상태 전이 흐름

```
not_requested (신청 안 함)
    ↓ (고객이 2단계에서 선택)
pending (승인 대기)
    ↓ (관리자 승인)
approved (승인됨)
    ↓ (4~14단계 진행)
in_progress (진행 중)
    ↓ (14단계 완료)
completed (완료)
```

---

## 4. 페이지별 역할

### 4.1 공통 프로세스 (1~3단계)

| 단계 | 페이지 | 역할 | 표시 위치 |
|------|--------|------|----------|
| 1 | valuation.html | 서비스 안내 | 홈 |
| 2 | project-create.html | 평가 신청 (여러 평가법 선택) | 홈 |
| 3 | approval-waiting.html | 관리자 승인 대기 | 홈 |

### 4.2 평가법별 프로세스 (4~14단계)

| 단계 | 페이지 | 역할 | 표시 위치 |
|------|--------|------|----------|
| 1 | guide-{method}.html | 서비스 안내 (평가법별) | 평가법 페이지 |
| 2 | → project-create.html | (홈으로 이동) | - |
| 3 | → approval-waiting.html | (홈으로 이동) | - |
| 4 | portal-{method}.html | 기초자료 제출 | 평가법 페이지 |
| 5 | data-collection.html | 데이터 수집 중 | 평가법 페이지 |
| 6 | evaluation-progress.html | 평가 진행 중 | 평가법 페이지 |
| 7 | accountant-review.html | 회계사 검토 중 | 평가법 페이지 |
| 8 | draft-generation.html | 초안 생성 중 | 평가법 페이지 |
| 9 | result-{method}.html?mode=draft | 초안 확인 | 평가법 페이지 |
| 10 | revision-request.html | 수정 요청 | 평가법 페이지 |
| 11 | final-preparation.html | 최종안 작성 중 | 평가법 페이지 |
| 12 | result-{method}.html?mode=final | 최종안 확인 | 평가법 페이지 |
| 13 | payment.html | 결제하기 | 평가법 페이지 |
| 14 | report-download.html | 보고서 수령 | 평가법 페이지 |

---

## 5. 작업 단계

### Phase 0-1: 데이터베이스 수정 ⭐ 최우선
```sql
-- projects 테이블에 평가법별 상태 필드 추가
ALTER TABLE projects
ADD COLUMN dcf_status TEXT DEFAULT 'not_requested',
ADD COLUMN dcf_step INTEGER DEFAULT 1,
ADD COLUMN relative_status TEXT DEFAULT 'not_requested',
ADD COLUMN relative_step INTEGER DEFAULT 1,
ADD COLUMN intrinsic_status TEXT DEFAULT 'not_requested',
ADD COLUMN intrinsic_step INTEGER DEFAULT 1,
ADD COLUMN asset_status TEXT DEFAULT 'not_requested',
ADD COLUMN asset_step INTEGER DEFAULT 1,
ADD COLUMN inheritance_tax_status TEXT DEFAULT 'not_requested',
ADD COLUMN inheritance_tax_step INTEGER DEFAULT 1;
```

### Phase 0-2: 공통 컴포넌트 생성

#### `frontend/app/components/project-status-checker.js`
```javascript
// 역할: 평가법별 상태 체크
export async function checkMethodStatus(projectId, method) {
    // dcf, relative, intrinsic, asset, inheritance_tax
    const { data } = await supabase
        .from('projects')
        .select(`${method}_status, ${method}_step`)
        .eq('id', projectId)
        .single();

    return {
        status: data[`${method}_status`],  // not_requested, pending, approved, in_progress, completed
        step: data[`${method}_step`]
    };
}
```

#### `frontend/app/components/common-sidebar.js`
```javascript
// 역할: 14단계 프로세스 사이드바 생성
export function renderSidebar(currentStep, methodStatus) {
    // 1~3단계: 항상 표시
    // 4~14단계: methodStatus가 'approved' 이상일 때만 활성화
}
```

### Phase 0-3: 홈 화면 수정 (valuation.html)

**추가할 내용:**
1. 사이드바에 3단계 프로세스 표시
2. "시작하기" 버튼 → project-create.html로 이동

### Phase 0-4: 평가 신청 페이지 수정 (project-create.html)

**변경 사항:**
```html
<!-- 기존: 라디오 버튼 (1개만 선택) -->
<input type="radio" name="method" value="DC">

<!-- 변경: 체크박스 (여러 개 선택) -->
<input type="checkbox" name="methods[]" value="dcf">
<input type="checkbox" name="methods[]" value="relative">
<input type="checkbox" name="methods[]" value="intrinsic">
<input type="checkbox" name="methods[]" value="asset">
<input type="checkbox" name="methods[]" value="inheritance_tax">
```

**JavaScript 수정:**
```javascript
// 프로젝트 생성 시
const selectedMethods = Array.from(
    document.querySelectorAll('input[name="methods[]"]:checked')
).map(cb => cb.value);

// Supabase 저장
const projectData = {
    company_name: companyName,
    project_id: projectId,
    status: 'pending',
    current_step: 2,
    // 선택된 평가법만 pending으로 설정
    dcf_status: selectedMethods.includes('dcf') ? 'pending' : 'not_requested',
    relative_status: selectedMethods.includes('relative') ? 'pending' : 'not_requested',
    intrinsic_status: selectedMethods.includes('intrinsic') ? 'pending' : 'not_requested',
    asset_status: selectedMethods.includes('asset') ? 'pending' : 'not_requested',
    inheritance_tax_status: selectedMethods.includes('inheritance_tax') ? 'pending' : 'not_requested'
};
```

### Phase 0-5: 승인 대기 페이지 생성 (approval-waiting.html)

**표시 내용:**
```html
<div class="approval-status">
    <h2>관리자 승인 대기 중</h2>

    <div class="method-status-list">
        <!-- 신청한 평가법들만 표시 -->
        <div class="method-item">
            <span class="icon">💰</span>
            <span class="name">현금흐름할인법 (DCF)</span>
            <span class="status approved">✅ 승인됨</span>
            <a href="guide-dcf.html?projectId=ABC">평가 진행하기 →</a>
        </div>

        <div class="method-item">
            <span class="icon">⚖️</span>
            <span class="name">상대가치평가법</span>
            <span class="status pending">🟡 승인 대기 중</span>
        </div>
    </div>
</div>
```

### Phase 0-6: 평가법별 페이지 수정 (guide-*.html)

**각 가이드 페이지 상단에 상태 체크 추가:**

```javascript
// guide-dcf.html
async function checkAccess() {
    const projectId = new URLSearchParams(window.location.search).get('projectId');
    const status = await checkMethodStatus(projectId, 'dcf');

    if (status.status === 'not_requested') {
        // "이 평가법은 신청하지 않았습니다" 표시
        showNotRequested();
        return;
    }

    if (status.status === 'pending') {
        // "관리자 승인 대기 중입니다" 표시
        showPendingApproval();
        return;
    }

    // approved, in_progress, completed → 정상 표시
    renderPage(status.step);
}
```

### Phase 0-7: 신규 페이지 생성 (8개)

**공통 특징:**
- URL 파라미터: `?projectId=ABC&method=dcf`
- 사이드바: 14단계 프로세스 표시
- 평가법 정보 표시

| 단계 | 파일명 | 설명 |
|------|--------|------|
| 5 | data-collection.html | AI 데이터 수집 중 |
| 6 | evaluation-progress.html | 평가 진행 중 |
| 7 | accountant-review.html | 회계사 검토 중 |
| 8 | draft-generation.html | 초안 생성 중 |
| 10 | revision-request.html | 수정 요청 |
| 11 | final-preparation.html | 최종안 작성 중 |
| 13 | payment.html | 결제하기 |
| 14 | report-download.html | 보고서 수령 |

---

## 6. 사용자 시나리오

### 시나리오 1: DCF + 상대가치 동시 신청

```
1. valuation.html 접속
   → "시작하기" 클릭

2. project-create.html
   → ✅ DCF 체크
   → ✅ 상대가치평가법 체크
   → "신청하기" 클릭

3. approval-waiting.html
   → DCF: 🟡 승인 대기 중
   → 상대가치: 🟡 승인 대기 중

4. 관리자가 DCF만 승인

5. approval-waiting.html (새로고침)
   → DCF: ✅ 승인됨 → "평가 진행하기" 버튼
   → 상대가치: 🟡 승인 대기 중

6. "평가 진행하기" 클릭 → guide-dcf.html
   → 4~14단계 진행 가능

7. 상대가치는 아직 승인 대기
   → guide-relative.html 접속하면 "승인 대기 중" 표시
```

### 시나리오 2: 추가 평가법 신청

```
1. DCF 진행 중 (8단계)

2. "상대가치평가법도 추가로 신청하고 싶어요"

3. valuation.html → "평가 추가 신청" 버튼

4. project-create.html (기존 프로젝트)
   → ✅ 상대가치평가법 체크
   → "추가 신청하기" 클릭

5. relative_status: pending으로 변경

6. DCF는 계속 진행 (8단계 → 9단계...)
   상대가치는 승인 대기
```

---

## 7. 장점 정리

### 7.1 실무적
```
✅ 여러 평가법 동시 신청 가능
✅ 평가법별 독립적 진행
✅ 하나는 진행, 하나는 대기 가능
✅ 추가 평가법 신청 가능
```

### 7.2 기술적
```
✅ 기존 15개 파일 그대로 사용 (통합 불필요!)
✅ 작업량 대폭 감소
✅ 명확한 상태 관리
✅ URL 구조 단순: guide-dcf.html?projectId=ABC
```

### 7.3 사용자 경험
```
✅ 홈: 신청 + 승인 (간단)
✅ 평가법별 페이지: 실제 작업 (명확)
✅ 신청 안 한 평가법 접속: "신청하세요" 안내
✅ 승인 안 된 평가법 접속: "승인 대기중" 안내
```

---

## 8. 예상 작업 기간

| Phase | 작업 | 예상 시간 |
|-------|------|----------|
| 0-1 | DB 수정 (필드 추가) | 10분 |
| 0-2 | 공통 컴포넌트 생성 | 1시간 |
| 0-3 | 홈 화면 수정 | 30분 |
| 0-4 | 평가 신청 페이지 수정 | 1시간 |
| 0-5 | 승인 대기 페이지 생성 | 1시간 |
| 0-6 | 평가법별 페이지 수정 (5개) | 2시간 |
| 0-7 | 신규 페이지 8개 생성 | 4시간 |
| **합계** | | **9시간 40분** |

**실제 작업 시간**: 1.5~2일 소요 예상 (테스트 포함)

---

## 9. 다음 단계

**Phase 0-1부터 시작**:
1. Supabase에서 projects 테이블 수정 (필드 추가)
2. `project-status-checker.js` 생성
3. `common-sidebar.js` 생성

**승인 후 작업 시작하겠습니다!** ✅
